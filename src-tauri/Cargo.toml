[package]
name = "driftgrid"
version = "0.1.0"
description = "DriftGrid — Design iteration for agents"
authors = ["BZY"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
